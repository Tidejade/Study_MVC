var Controller={};

(Controller.users=function($){
	var nameClick=function(){

	};
$(function(){
	$("#view.name").click(nameClick);
});
})(jQuery);

//The Controller receive events and input from views,process them , and update the views accordingly.
//Controllers are the glue between models and views.