/*
// The bellow code is not namespaced or object oriented.
var user=users["foo"];
destoryUser(user);
*/

var user=users.find("foo");
user.destory();

/*The model is where all the application's data objects are stored.
  The only thing a model should contain is data and the logic associated directly with that data.
  When controllers fetch data from servers or create new records, they wrap them in model instances.
*/
/////TODY IS 2017.1.1
